{
  "name": "project-sanctum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "version": "node scripts/version.cjs",
    "prebuild": "npm run version"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "phaser": "^3.80.0"
  }
}
